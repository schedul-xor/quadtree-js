require('nclosure').nclosure({additionalDeps: ['deps.js']});
expect = require('expect.js');

goog.require('schedul.qt.Node');

describe('schedul.qt.Node, general', function() {
  it('should return build valid tree',function(){
  });
});
